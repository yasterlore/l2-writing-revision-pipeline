# Synthetic Safe-View Fixtures

These fixtures are synthetic `NoOracleSafeEpisodeView` JSONL examples for Python candidate generation tests.

They are not participant data, experiment data, or production data. Do not place real participant exports in this directory.

`valid/` contains no-oracle-safe examples. `invalid/` contains intentionally unsafe examples for rejection tests.
