# Synthetic Hand-Weight Config Fixtures

These fixtures exercise the hand-weight config schema and validator.

They are synthetic design fixtures only. They are not connected to scoring and
must not be treated as learned weights, performance results, or real-data
configuration.

Valid fixtures should describe current-default-like behavior only. Invalid
fixtures intentionally contain malformed or unsafe config shapes for validator
tests.
