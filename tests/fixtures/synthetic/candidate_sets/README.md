# Synthetic CandidateSet Fixtures

These fixtures are synthetic `CandidateSet` JSONL examples for candidate feature extraction tests.

They are not participant data, experiment data, or production data. Do not place real participant candidate outputs in this directory.

`valid/` contains no-oracle-safe examples. `invalid/` contains intentionally unsafe examples for rejection tests.
