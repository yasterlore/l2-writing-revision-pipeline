{
  "name": "kslog-logger-web",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal synthetic-only browser raw event logger for keystroke-level writing process research.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node dist-test/tests/rawEvent.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
